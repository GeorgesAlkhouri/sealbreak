# PR #14 CI Repair Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Repair PR #14 so its tests, iOS build, Sonar quality gate, and aggregate ready gate pass without weakening the TCA architecture.

**Architecture:** Keep `AppFeature` and the feature reducers as the owners of state, actions, effects, navigation, and injected dependencies. Limit changes to test-driving asynchronous reducer effects correctly, selecting the simulator through Xcode's destination mechanism, and expressing full-intensity color components without redundant arithmetic.

**Tech Stack:** Swift 5 language mode, Swift Testing, The Composable Architecture 1.26.1, Xcode 26.6, GitHub Actions, SonarQube Cloud.

**Spec:** User request in the Codex task continuing “Branch · UI Konzept entwerfen”.

## Global Constraints

- Keep `AppModel` removed.
- Keep TCA responsible for state, actions, effects, navigation, and dependencies.
- Keep OpenBao, Keychain, and Face ID infrastructure behind dependencies.
- Preserve the existing UI colors and behavior.
- Push fixes to the verified PR head branch `refactor/tca`.
- Preserve the pre-existing untracked Xcode workspace `Package.resolved`; do not include it in this repair commit.

---

### Task 1: Drain non-exhaustive TCA test effects

**Files:**
- Modify: `Tests/SealbreakCoreTests/FeatureTests.swift`
- Test: `Tests/SealbreakCoreTests/FeatureTests.swift`

**Interfaces:**
- Consumes: TCA `TestStoreTask.finish()` and `TestStore.skipReceivedActions()`.
- Produces: Final reducer state after asynchronous dependency responses have been reduced.

- [ ] **Step 1: Verify the existing regression**

Run: `swift test --enable-code-coverage -Xswiftc -warnings-as-errors`

Expected: the asynchronous feature tests fail because received actions have not been applied to the non-exhaustive test stores.

- [ ] **Step 2: Apply queued actions after each completed effect**

For each non-exhaustive asynchronous flow, keep the action task completion and then apply queued received actions:

```swift
await store.send(.refreshTapped).finish()
await store.skipReceivedActions()
```

- [ ] **Step 3: Run the full Swift test suite**

Run: `swift test --enable-code-coverage -Xswiftc -warnings-as-errors`

Expected: all Swift tests pass and `coverage.lcov` can be generated by the workflow command.

### Task 2: Build package macros for the host

**Files:**
- Modify: `scripts/ci/build.sh`
- Test: `scripts/ci/build.sh`

**Interfaces:**
- Consumes: Xcode's `generic/platform=iOS Simulator` destination selection.
- Produces: An unsigned simulator app while Swift package macro plugins remain macOS host executables.

- [ ] **Step 1: Verify the existing regression**

Run: `bash scripts/ci/build.sh simulator`

Expected: build fails with `DYLD_ROOT_PATH not set for simulator program` and malformed TCA macro responses.

- [ ] **Step 2: Remove global target overrides**

Use the destination to select the simulator SDK and architecture:

```bash
xcodebuild "${common[@]}" -configuration Debug \
  -destination 'generic/platform=iOS Simulator' \
  -derivedDataPath build/Simulator \
  build
```

- [ ] **Step 3: Run the CI build entry point**

Run: `bash scripts/ci/build.sh simulator`

Expected: `** BUILD SUCCEEDED **` with macro executables loaded from the host products directory.

### Task 3: Restore Sonar reliability rating

**Files:**
- Modify: `Sealbreak/DesignSystem/Papercut/PapercutPalette.swift`
- Test: SonarQube Cloud analysis in GitHub Actions.

**Interfaces:**
- Consumes: SwiftUI `Color` components in the closed range `0...1`.
- Produces: The exact same red channel values for `sealed` and `sun` without identical operands around `/`.

- [ ] **Step 1: Verify the Sonar issues**

Query PR #14's open Sonar bugs.

Expected: two `swift:S1764` issues at the `255 / 255` red components.

- [ ] **Step 2: Express full intensity directly**

Replace each `red: 255 / 255` with:

```swift
red: 1
```

- [ ] **Step 3: Run local quality checks**

Run: `pre-commit run --all-files --show-diff-on-failure`

Expected: all hooks pass.

### Task 4: Verify and publish

**Files:**
- Verify all modified files.

**Interfaces:**
- Consumes: Tasks 1–3.
- Produces: One reviewed commit on `refactor/tca` and green PR checks.

- [ ] **Step 1: Run all local gates from clean inputs**

Run the Swift tests with coverage and pre-commit. Remove only `build/Simulator`, then run the CI simulator build so no prior Derived Data can mask the result.

Expected: every command exits successfully.

- [ ] **Step 2: Review the diff for architecture regressions**

Confirm no `AppModel` reintroduction and no direct infrastructure ownership in views or reducers outside dependency injection.

- [ ] **Step 3: Commit and push**

```bash
git add Tests/SealbreakCoreTests/FeatureTests.swift scripts/ci/build.sh Sealbreak/DesignSystem/Papercut/PapercutPalette.swift docs/superpowers/plans/2026-09-17-pr-14-ci-repair.md
git commit -m "fix(ci): repair TCA test and build gates"
git push origin refactor/tca
```

Keep `Sealbreak.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved` untracked because it predates this task.

- [ ] **Step 4: Verify PR checks**

Wait for PR #14's `CI / quality`, `CI / tests`, `CI / build`, `CI / sonar`, and `CI / ready` checks.

Expected: every required check succeeds.
